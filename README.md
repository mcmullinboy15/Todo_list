# Todo_list

This Project is not in production.

Django project using django's templating engine to service the fronted and backend, with some basic Vue functions in the frontend

Deployed on Heroku Server with Automatic `git push` deployment
